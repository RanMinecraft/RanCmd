name: RanOnlineApi
main: cc.ranmc.api.Main
description: '阿然定制24k纯钛合金超级无敌终终终终极版的查询在线插件'
website: https://www.ranmc.cc/
author: Ranica
api-version: "1.13"
version: Release 2.2
folia-supported: true


permissions:
  roa.user:
    default: true
    description: 'user only'

  roa.admin:
    default: op
    description: 'admin only'

  roa.joinfullserver:
    default: op
    description: 'join full server'

commands:
  roa:
    description: roa
    usage: /<command>

  ad:
    description: ad
    usage: /<command>

  vai:
    description: vai
    usage: /<command>